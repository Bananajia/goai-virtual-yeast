"""Aggregate-only evidence registry data.

The executable Interface lives in ``experiment_core.legacy_evidence``. Keeping the
registry declarative makes the status of historical results inspectable without
importing any private-data loader.
"""
